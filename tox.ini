[tox]
envlist = py34, py36, flake8, docs

[travis]
python =
    3.6: py36
    3.4: py34

[testenv:flake8]
basepython = python
deps =
    -r{toxinidir}/requirements_dev.txt
commands = flake8 wespe

[testenv:docs]
basepython=python
changedir=docs
deps =
    -r{toxinidir}/requirements_dev.txt
commands=
    sphinx-build -b html -d {envtmpdir}/doctrees . {envtmpdir}/html

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements_dev.txt
    -r{toxinidir}/requirements.txt
commands =
    pip install -U pip
    py.test --basetemp={envtmpdir}
